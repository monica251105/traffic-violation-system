# ============================================================
# requirements.txt
# Sistem Deteksi Pelanggaran Lalu Lintas
# ============================================================

# Core ML & Computer Vision
ultralytics>=8.0.0        # YOLOv8 (includes torch, torchvision)
opencv-python>=4.8.0      # OpenCV untuk pemrosesan video/gambar
flask>=2.2.0
flask-cors>=3.0.10
pyngrok>=5.2.0

# (Opsional) GPU acceleration — hapus komentar jika pakai NVIDIA GPU
# torch==2.0.0+cu118
# torchvision==0.15.0+cu118

python-dotenv>=1.0.0
Pillow>=10.0.0            # Manipulasi gambar
pandas>=2.0.0             # Analisis data log CSV
matplotlib>=3.7.0         # Visualisasi grafik (opsional)
